# Only needed when running React separately from Flask (npm start on port 3000).
# In production (npm run build), React is served by Flask at /api — leave blank.
REACT_APP_API_URL=http://localhost:5000/api
